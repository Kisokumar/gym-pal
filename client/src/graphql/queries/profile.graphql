query Profile($username: String!) {
  profile(username: $username) {
    owner
    errors {
      field
      message
    }
    user {
      username
      privateAccount
      hideConnections
      createdAt
      updatedAt
    }
  }
}
